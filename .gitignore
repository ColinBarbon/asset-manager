# Dependencies
node_modules/

# Runtime database (auto-created and seeded on first run)
assets.db

# Logs
*.log
npm-debug.log*

# Environment / secrets
.env
.env.*
# keep the (secret-free) template in the repo
!.env.example

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
